<div class="d-flex flex-row bd-highlight mb-3">
  <h1 class="bd-highlight">Sintomas</h1>
  <%= link_to 'Voltar', root_path, class: "m-3 bd-highlight" %>
</div>

<% if @symptoms.any? %>
  <table class="table table-striped table-edit">
    <thead>
      <tr>
        <th>Nome</th>
        <% if current_user.role == 'admin' %>
          <th>Usuário</th>
        <% end %>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @symptoms.each do |symptom| %>
        <tr>
          <td><%= symptom.name %></td>
          <% if current_user.role == 'admin' %>
            <td>
              <%= symptom.user.name %>
            </td>
          <% end %>
          <td>
            <%= link_to edit_symptom_path(symptom) do %>
              <i class="fas fa-edit"></i>
            <% end %>
          </td>
          <td>
            <%= link_to symptom, method: :delete, data: { confirm: 'Tem certeza?' } do %>
              <i class="fas fa-times-circle red"></i>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  Não há sintoma cadastrado
<% end %>

<br>

<%= link_to new_symptom_path, class: "btn btn-success btn-margin-bottom" do %>
  Nova sintoma
<% end %>
