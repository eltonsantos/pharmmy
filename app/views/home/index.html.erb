<h1>PharmMy</h1>

<% if @medicine.last == nil and @symptom.last == nil %>
  <p>
    Seja bem vindo a PharmMy. Sua farmacinha particular e online!
    No momento você não tem nada cadastrado no sistema.

    <p>Para começar a usar a PharmMy comece cadastrando um sintoma <%= link_to "aqui", new_symptom_path %></p>
  </p>
<% else %>  
  <p>Lista de remédios que tenho em casa | Atualizado em:
    <span class="badge bg-dark">
      <%= @medicine.last ? @medicine.last.updated_at.strftime("%d/%m/%Y") : "---" %>
    </span> 
  </p>

  <br />

  <div class="row btn-margin-bottom">
    <div class="col-4">
      <%= link_to medicines_path, class: "btn btn-primary" do %>
        Remédios cadastrados
        <span class="badge bg-secondary">
          <%= @medicine.count %>
        </span>
      <% end %>
    </div>
    <div class="col-4">
      <%= link_to symptoms_path, class: "btn btn-info" do %>
        Sintomas cadastrados
        <span class="badge bg-secondary">
          <%= @symptom.count %>
        </span>
      <% end %>
    </div>
    <div class="col-4">
      <button type="button" class="btn btn-warning pe-none">
         Próximo ao vencimento
         <span class="badge bg-secondary">
          0
         </span>
      </button>
    </div>
  </div>
<% end %>